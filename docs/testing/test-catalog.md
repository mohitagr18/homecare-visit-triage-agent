# Test Catalog

This document lists all 55 automated tests running in the pipeline. They are organized by the Four-Layer Testing Architecture. For a high-level explanation of these layers, see `docs/testing/overview.md`.

| Layer | Test Name | What it checks | Why it matters | Real Example |
|---|---|---|---|---|
| 1. Unit | `test_exact_match_passes` | That exactly matching strings score 1.0. | Establishes base equality functionality. | `"foo"` matches `"foo"` and returns a score of 1.0. |
| 1. Unit | `test_unequal_strings_fail` | That non-matching strings score 0.0. | Prevents false positives in text evaluation. | `"foo"` compared to `"bar"` returns a score of 0.0. |
| 1. Unit | `test_both_none_pass` | That two empty (None) values score 1.0. | Null values in ground truth and extraction mean a successful match. | `None` compared to `None` returns a score of 1.0. |
| 1. Unit | `test_one_none_fails` | That one empty value against a populated value scores 0.0. | Prevents silent passes when extractions are dropped. | `None` compared to `"foo"` returns a score of 0.0. |
| 1. Unit | `test_exact_match_passes` (Hours) | That exactly matching floats score 1.0. | Baseline metric scoring works. | 8.0 hours matches 8.0 hours and returns 1.0. |
| 1. Unit | `test_at_tolerance_boundary_passes` | That a difference exactly equal to the tolerance passes. | Borderline acceptable data is not unfairly penalized. | 7.0 hours compared to 7.25 hours passes because the difference is exactly 15 minutes. |
| 1. Unit | `test_one_minute_over_boundary_fails` | That a difference slightly over the tolerance fails. | Enforces strict mathematical limits on LLM errors. | 7.0 hours compared to 7.26 hours strictly fails because 15.6 minutes exceeds the 15-minute tolerance. |
| 1. Unit | `test_under_tolerance_boundary_passes` | That a difference strictly inside the tolerance passes. | Normalizes minor mathematical inaccuracies. | 7.0 hours compared to 7.20 hours passes because 12 minutes is under the 15-minute limit. |
| 1. Unit | `test_negative_diff_same_as_positive` | That absolute difference is checked regardless of order. | Penalizes over-extraction and under-extraction equally. | 7.25 compared to 7.0 passes just like 7.0 compared to 7.25. |
| 1. Unit | `test_zero_hours_passes_zero_gt` | That empty or 0.0 floats are correctly matched. | Validates zero-hour timesheets mathematically. | 0.0 hours compared to 0.0 hours passes. |
| 1. Unit | `test_large_diff_fails` | That massive differences strictly fail. | Prevents wild LLM hallucinations from scoring. | 7.0 hours compared to 9.0 hours fails with a score of 0.0. |
| 1. Unit | `test_exact_match_passes` (Time) | That matching datetime.time objects score 1.0. | Base functionality for the Time In/Out metric. | `08:00` compared to `08:00` returns 1.0. |
| 1. Unit | `test_at_tolerance_boundary_passes` (Time) | That exactly matching the time tolerance limit passes. | Borderline time extractions are accepted safely. | `08:00` compared to `08:30` passes exactly at the ±30 minute tolerance boundary. |
| 1. Unit | `test_one_minute_over_boundary_fails` (Time) | That exceeding the time tolerance strictly fails. | Enforces strict limits on time drift. | `08:00` compared to `08:31` fails. |
| 1. Unit | `test_earlier_time_symmetric` | That earlier times are penalized equally to later times. | Prevents directional bias in evaluation. | `08:30` compared to `08:00` passes just like `08:00` compared to `08:30`. |
| 1. Unit | `test_large_diff_fails` (Time) | That massive time gaps fail. | Catches severe AM/PM mixups or hallucinations. | `07:00` compared to `15:00` fails. |
| 1. Unit | `test_valid_row_normalizes_correctly` | That valid string inputs convert to python types. | Ensures evaluation arithmetic can run correctly. | The string `"08:30"` successfully normalizes to `datetime.time(8, 30)`. |
| 1. Unit | `test_source_file_preserved_as_anonymized` | That anonymized filenames are not mutated to leak PHI. | Protects privacy early in the pipeline. | The real name "Rivera" is asserted to NOT be present in the normalized file name `patient_a_week1.pdf`. |
| 1. Unit | `test_empty_time_in_returns_none` | That empty time strings map safely to None. | Prevents parser crashes on blank fields. | Providing `time_in=""` safely returns `None`. |
| 1. Unit | `test_empty_time_out_returns_none` | That empty time out strings map safely to None. | Prevents parser crashes on blank fields. | Providing `time_out=""` safely returns `None`. |
| 1. Unit | `test_invalid_date_returns_none` | That malformed date strings are caught. | Prevents `datetime` crashes down the line. | Providing `date="not-a-date"` safely returns `None`. |
| 1. Unit | `test_invalid_time_returns_none` | That impossible times are caught. | Protects against LLM hallucinations generating invalid times. | Providing `time_in="25:99"` safely returns `None`. |
| 1. Unit | `test_routes_to_human_review_when_flagged` | That the conditional edge routes to human review if requested. | Ensures flagged data isn't evaluated silently. | A state where `needs_review=True` correctly returns the `"human_review"` node name. |
| 1. Unit | `test_routes_to_report_when_clean` | That clean data routes straight to report. | Optimizes pipeline speed for perfect data. | A state where `needs_review=False` correctly returns the `"report"` node name. |
| 1. Unit | `test_routes_to_report_when_needs_review_false_despite_flagged` | That explicit bypass flags override internal flags. | Ensures defensive routing. | If `needs_review=False` is forced, it overrides the presence of flagged rows and returns `"report"`. |
| 1. Unit | `test_routes_to_report_when_flagged_list_empty` | That an empty flag list is treated as clean. | Fallback safety net. | An empty flagged list safely returns `"report"`. |
| 1. Unit | `test_normalized_row_never_contains_real_name` | That real patient names are mathematically excluded from processing. | Hard PHI constraint. | The test asserts the real name "Hanton" does not accidentally leak into `patient_l_week5.pdf`. |
| 1. Unit | `test_row_eval_source_file_format` | That evaluation IDs are prefixed properly. | Ensures consistent anonymization schemas. | The test asserts the source file strictly starts with the `"patient_"` prefix. |
| 2. Integration | `test_clean_file_completes_without_review` | That perfect data bypasses the HITL gate seamlessly. | Validates the "happy path" of the state machine. | The `clean_merged.xlsx` fixture runs the graph and returns `needs_review=False` without pausing. |
| 2. Integration | `test_state_populated_through_all_nodes` | That no data is silently dropped between nodes. | Prevents denominator manipulation. | The test asserts that `extraction_rows` and `normalized_rows` are populated and not empty after execution. |
| 2. Integration | `test_output_artifacts_written` | That the pipeline physically saves files to disk. | Proves artifacts are actually generated. | The test asserts `paper_table.md` successfully exists in the `summary/` output directory. |
| 2. Integration | `test_output_contains_only_anonymized_filenames` | That final artifact files are 100% devoid of known PHI. | The ultimate privacy guarantee for the paper. | The test scans all generated `*.json` output files to explicitly assert names like "Rivera" are absent. |
| 2. Integration | `test_ambiguous_rows_pause_graph` | That ambiguous rows physically halt the LangGraph. | Proves the state machine respects boundaries. | Running the `ambiguous_merged.xlsx` fixture causes the graph's `snapshot.next` to equal `("human_review",)`. |
| 2. Integration | `test_needs_review_set_true_when_flagged` | That internal graph state is updated correctly by triage. | Ensures downstream nodes know the context. | Running `ambiguous_merged.xlsx` correctly updates `needs_review=True` in the final dictionary state. |
| 2. Integration | `test_gt_hours_accuracy_is_float_between_0_and_1` | That the core metric is structurally valid. | Prevents math errors from crashing the report generator. | The test asserts `gt_hours_accuracy` is mathematically `>= 0.0` and `<= 1.0`. |
| 2. Integration | `test_paper_table_contains_method_row` | That the method name is injected into the markdown. | Validates table formatting. | The string `"test_method"` is asserted to appear in the generated `paper_table.md` output. |
| 3. HITL | `test_ambiguous_rows_trigger_interrupt` | That the `interrupt_before` logic triggers correctly. | Ensures humans get a chance to review. | The test asserts the graph halts specifically at the `("human_review",)` node. |
| 3. HITL | `test_flagged_rows_in_state_at_interrupt` | That review rows are populated before halting. | Ensures humans actually have data to review. | The `flagged_for_review` list is asserted to have a `length > 0` precisely at the interrupt boundary. |
| 3. HITL | `test_eval_results_in_state_at_interrupt` | That evaluation math is done before asking humans. | Gives humans context (what math passed/failed) to make a decision. | The `eval_results` array is already populated in state before the human interrupt fires. |
| 3. HITL | `test_summary_not_yet_written_at_interrupt` | That reports aren't written until humans finish. | Prevents incomplete data from reaching the paper table. | The `summary` field is asserted to be `None` because the report node hasn't run yet. |
| 3. HITL | `test_clean_data_does_not_trigger_interrupt` | That good data never bothers the human reviewer. | Proves the system scales efficiently. | Running `clean_merged.xlsx` fully completes with `next=()` on the very first invocation. |
| 3. HITL | `test_graph_completes_after_resume` | That the graph wakes up and generates reports upon resume. | Proves the LangGraph is mechanically robust. | Injecting the `Command(resume=[...])` payload successfully triggers the report node to run. |
| 3. HITL | `test_human_decisions_stored_in_state` | That the human's exact decision is logged. | Audit trail for overrides. | The reviewer's `accept=True` boolean is successfully stored in the `human_decisions` state field. |
| 3. HITL | `test_artifacts_written_after_resume` | That files are written specifically *after* the resume. | Proves the final output includes human input. | The `run_summary.json` file is physically created on disk only after the resume completes. |
| 3. HITL | `test_graph_fully_complete_after_resume` | That the graph terminates cleanly. | Prevents hanging threads. | The state snapshot returns `next=()` after the resume payload is successfully injected. |
| 4. Evaluation | `test_exact_hours_match_scores_1` | That synthetic normalized data matches synthetic ground truth. | End-to-end arithmetic check. | A synthetic row with 8.0 hours perfectly matches a Ground Truth row with 8.0 hours, scoring 1.0. |
| 4. Evaluation | `test_hours_within_tolerance_scores_1` | That tolerance applies accurately to synthetic data. | End-to-end tolerance check. | A normalized row with 8.25 hours (+15 min) successfully scores 1.0 against an 8.0 hour GT. |
| 4. Evaluation | `test_hours_over_tolerance_scores_0` | That over-tolerance data fails end-to-end. | End-to-end strict boundary check. | A normalized row with 8.5 hours (+30 min) strictly fails the ±15 min tolerance, scoring 0.0. |
| 4. Evaluation | `test_no_gt_match_produces_unmatched_result` | That unmapped rows are handled gracefully. | Prevents crashes on data missing from ground truth. | Calling `evaluate_row` with `None` GT correctly returns `matched_gt=False`. |
| 4. Evaluation | `test_eval_result_source_file_is_anonymized` | That evaluation results map back to anonymized IDs. | The final link in the PHI isolation chain. | The `RowEvalResult` retains `patient_a_week1.pdf` and never publicly associates with the real GT filename. |
| 4. Evaluation | `test_at_least_one_file_has_gt_coverage` | That the dataset actually has overlap. | Catch-all test if name resolution totally breaks. | `evaluate_file` returns a list with `len() > 0`, proving the DB resolver successfully mapped patients. |
| 4. Evaluation | `test_gt_hours_accuracy_above_minimum_threshold` | That the real VLM models meet baseline performance. | Regression guard against model decay. | The test physically asserts that the final `band_crop_vlm_cloud` accuracy float is `>= 0.60`. |
| 4. Evaluation | `test_no_phi_in_eval_results` | That real data outputs are scrubbed. | Final safety check on actual production dataset outputs. | Scans `FileEvalResult.source_file` to explicitly assert the real name "Pegram" is completely absent. |
| 4. Evaluation | `test_fully_correct_rate_above_zero` | That at least one row was perfect. | Catches systemic offset bugs (like timezones). | Asserts the total fully correct rows sum is `> 0`. |
| 4. Evaluation | `test_failures_json_contains_only_anonymized_ids` | That failure debug logs don't leak PHI. | Protects developer logs. | Parses `failures.json` to explicitly assert the name "Derricott" does not appear in the string dump. |
